Vendored from `actions/cache` `v4.2.4`.

This repository is configured with `allowed_actions=local_only`, so external
GitHub Actions such as `actions/cache/*` cannot be referenced directly.

Keep this local action in sync with the upstream release when updating cache
behavior or runtime support.
