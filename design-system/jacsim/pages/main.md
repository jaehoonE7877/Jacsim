# Main Page Overrides

> Applies to: `Projects/Jacsim/Sources/Presentation/Main/MainView.swift`

## Layout Overrides

- `MainView`는 Home container로 단순 유지
- 탭/라우팅 구조 도입 시에도 화면 스타일 책임은 하위 Feature에 둠

## Component Overrides

- Main 레벨에서 중복 네비게이션 컨테이너 생성 금지
