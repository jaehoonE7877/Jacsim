# Challenge Create Page Overrides

> Applies to: `Projects/Jacsim/Sources/Presentation/ChallengeCreate/ChallengeCreateView.swift`

## Layout Overrides

- 신규 화면 확장 없이 `NewTaskView` 흐름을 재사용
- 모달 맥락에서 CTA 우선순위(저장 > 취소) 유지

## Component Overrides

- 별도 시각 규칙 추가 금지
- `new-task` override를 그대로 상속
