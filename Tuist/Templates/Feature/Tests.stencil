import Testing
import ComposableArchitecture

@testable import Jacsim

@MainActor
@Test("{{ name }} 기본 액션이 전송된다")
func {{ name|lower }}Feature_onAppear() async {
    let store = TestStore(initialState: {{ name }}Feature.State()) {
        {{ name }}Feature()
    }

    await store.send(.onAppear)
}
